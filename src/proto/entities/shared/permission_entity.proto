syntax = "proto3";

message PermissionScope {
    string scope = 1;
    repeated string subPermissions = 2;
}

message PermissionEntity {
    string application = 1;
    repeated PermissionScope permissions = 2;
}